---

spring:
  application:
    name: company
  cloud:
    config:
      uri: ${vcap.services.cfgserverrepo.credentials.cfguri}

management:
  security:
    enabled: false

security:
  basic:
    enabled: false

